<div id="tracks-search">
    <h1>Tracks</h1>
    <ul>
    <% tracksResult.forEach((track) => { %> 
    <h2><%-track.name%></h2>
    <audio controls src="<%- track.preview_url %>">
        <button><a href="<%-track.preview_url%>"">Play Audio</a></button>
    </audio>
    <%})%>
    </ul>
  </div>